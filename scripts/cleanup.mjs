// One-off cleanup script — ALREADY RUN on 2026-06-14, no longer needed.
// The destructive delete has been removed so this cannot wipe data if run again.
// Safe to delete this whole `scripts/` folder:  rm -rf scripts
console.log('This one-off cleanup script has already been run and is now a no-op.')
console.log('Delete it with: rm -rf scripts')
